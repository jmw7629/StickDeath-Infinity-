# Selection Flip H and Flip V

The existing Move popup now reflects explicitly selected drawing elements around their combined document-space bounds. Both axes use the same strict typed Studio command as the UI. Original brush points, seeds, shape metadata, sparse fill coverage, IDs, element order and layers remain editable. Schema8 stores an optional reflection plus the existing bounded translation; historical elements retain their prior encoding and older readers must reject the newer format.

Each completed flip is one reversible document edit. Empty, duplicate, missing, stale, hidden, transparent or locked selections fail without mutation. Every cancellation checkpoint and an invalid multi-command batch roll back. Bounds are finite, translations remain within100,000 units per axis and a command addresses at most1,024 existing elements. Fill hit testing applies the inverse transform, so later selection and Move use actual reflected coverage. Production save/reopen, frame clipboard, layer duplication and the canonical thumbnail/export renderer preserve transforms. Imported images are not part of this selection operation.

## Actual local verification

Ten production groups passed using the real model, typed executor, view model, device storage, SwiftUI renderer, decoded PNG and actual H.264 export. They cover both axes, group bounds, Undo/Redo, historical encoding, double flip, existing translations, cold reopen, frame clipboard/layer copies, sparse fill coverage and inverse hit testing, all ten brush families, locks/invalid schema, cancellation and wire/batch rollback.

The movie regression creates two distinct frames through the production frame clipboard, reflects their editable artwork and invokes the real movie exporter. AVAssetReader decodes every encoded frame. The test verifies H.264, canvas dimensions, both frame identities, exact presentation timestamps, total duration, expected red/blue positions and clearing of the original positions. It compares every decoded RGB pixel with the canonical white-composited render; measured mean errors were1.4605 and1.2089 out of255 against a limit of8. The exported file was1,598bytes and its manifest byte count matched the actual file. This animation-only fixture requires no audio track and verifies output cleanup. Separate existing audio-mix regression coverage remains in force.

The exact production and test input hashes for63 previously passing regressions match this candidate:11 Move,26 audio-mix,15 fill,8 ordering and3 model cases. The127-source iOS SDK/seven-primary-body check and23 UI definitions also have byte-identical inputs. Those unchanged suites were not needlessly rerun for a test-and-documentation-only addition. Together with the ten newly executed Flip groups, the source has73 actual production/regression checks supported by preserved exit codes, logs and input hashes. This does not replace the pending full native run.

The first raster comparison failed because reflected vector corners are rerasterized by Apple rendering. The measured fixture differed at32 channels, at most40/255, on16 antialiased corner pixels. Its original failure and source are retained. The final comparison requires exact interior pixels, permits differences only within one pixel of an alpha edge, and caps them at64 channels and64/255. It also requires unchanged original geometry, exact double-flip fixture pixels and exact decoded PNG agreement with the production renderer. This is not a claim of universal pixel parity. A separate standalone compile exposed and then verified removal of an unwanted document-error dependency from the lightweight model source set.

## Native and release gates

The twenty-third native journey is defined to draw an asymmetric stroke, use the actual Flip H/V controls, inspect mirrored positions, Undo/Redo, save, terminate and cold reopen with matching pixels. It has not run. The app build, full simulator suite, independent review, physical devices and release remain unverified for this slice. Move21 is fully green; ordering22 evidence must finish before publication. No new toolbar, provider key, network endpoint or live publication behavior is added.

Flip success preserves the existing status instead of inserting a canvas-resizing banner. The new production assertion reproduced this defect against the immutable previous code, and checks existing-error preservation on the second axis. Native assertions check unchanged canvas bounds immediately after both buttons, before save. Updated after-correction verification is recorded separately; native23 remains unrun.

The movie regression changes no application code. Original before failures, prior frozen candidates and all verification provenance remain preserved. Native23 and physical-device behavior are not yet verified.
