# Tool options fit their content

The sole existing tool popup now uses the natural height of short settings such as Hand/Zoom. Long content still scrolls within the same bounded viewport. A popup above a low floating rail stays adjacent to the rail; the default top rail keeps its popup below. The white snapping toolbar, canvas layout, close target, document/history and tools are unchanged. There is no secondary toolbar.

An original native capture from the fully passing216 build exposed a large empty Hand viewport. A first ViewThatFits change compiled but an actual SwiftUI render still measured482 dark card rows in the492-point container. That source and failed render are preserved. The correction uses a small SwiftUI Layout to measure ideal content height before supplying a bounded ViewThatFits proposal. This prevents the flexible max-height frame from expanding short settings.

Before the later d0804aa Move-guidance correction, the actual production panel and23 dependent sources compiled and rendered on the Intel Mac. The final Hand card extent was188points, aligned at the top or bottom of the same492-point workspace; short-screen Hand was108points in128, and default Brush was298points. Original images, source hashes and bounding measurements are preserved. AppKit ImageRenderer does not render interactive platform controls; those diagnostic images are not a native iOS screenshot or proof of button appearance, scrolling or touch. They must not be shown as a product preview.

After carrying the d0804aa Move correction into this candidate, the127-source iOS SDK check with the two changed primary bodies passed again. The preceding Mac layout captures retain their earlier source hashes; they were not rerendered on this updated source. All23 UI definitions type-checked. The existing native toolbar journey now asserts a compact Hand card, reachable44-point close target and adjacency without a new test count or longer timeout. That new runtime assertion has not executed. Full native build/journeys, actual long-library scrolling, iPad/physical layout and independent review remain pending. This private slice follows the separately verified Move, ordering and Flip sequence.
